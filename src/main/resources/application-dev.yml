spring:
  datasource:
    username: root
    password: ljfwww666
    url: jdbc:mysql://localhost:3306/tinyblog?useUnicode=true&characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    database: mysql
    hibernate:
      ddl-auto: update

logging:
  level:
    root: info # warn
    com.example.tinyblog: debug
  file:
    path: log/blog-dev